import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>Trivia Trove</h1>
      {/* Add navigation links, logo, or other header content */}
    </header>
  );
};

export default Header;